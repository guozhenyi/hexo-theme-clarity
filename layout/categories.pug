extends base

block title
  title= page.title + ' | ' + config.title

block content
  .post
    h1.post-title= page.title
    .post-content
      ul
        each category in site.categories.toArray()
          li
            a(href=url_for(category.path))= category.name
            span.item-count= category.length
